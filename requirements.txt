pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
matplotlib>=3.7

# Optional — only needed if you run scripts/fetch_kaggle_data.py to pull
# real data from Kaggle instead of using the bundled sample dataset.
kaggle>=1.6
